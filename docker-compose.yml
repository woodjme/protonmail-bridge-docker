services:
  protonmail-bridge:
    image: ghcr.io/woodjme/protonmail-bridge-docker:latest
    ports:
      - 1025:25/tcp
      - 1143:143/tcp
    restart: unless-stopped
    volumes:
      - protonmail:/root

volumes:
  protonmail:
    name: protonmail
